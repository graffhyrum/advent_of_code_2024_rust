use crate::days::utils::parse_input;
use crate::problems::Problem;


pub struct DayTen;

impl Problem for DayTen {
    fn part_one(&self, input: &str) -> String {
        input.to_string()
    }
    fn part_two(&self, input: &str) -> String {
        input.to_string()
    }
}

